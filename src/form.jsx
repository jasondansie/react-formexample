import './form.css';

const Form = (props) => {
    return (
        <form className='form'>
            <div>
                <label htmlFor="firstName"></label>
            </div>

        </form>
    );
}